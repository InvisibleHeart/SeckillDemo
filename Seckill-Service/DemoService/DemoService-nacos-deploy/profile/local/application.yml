server:
  port: 31018


spring:

  #redis相关
  redis:
    enable: false
    database: 0
    host: 39.101.168.64
    port: 39000
    password: 123456   # 如果未单独配置默认为空即可
    timeout: 3000
    jedis:
      pool:
        max-active: 8 # 连接池最大连接数（使用负值表示没有限制）
        max-wait: -1 # 连接池最大阻塞等待时间（使用负值表示没有限制）
        max-idle: 8 # 连接池中的最大空闲连接
        #连接池中的最小空闲连接
        min-idle: 0  #连接池中的最小空闲连接